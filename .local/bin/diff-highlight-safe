#!/bin/sh

exists() {
  if command -v $1 2>&1 > /dev/null; then
    return 0
  else
    return 1
  fi
}

if exists diff-highlight; then
  diff-highlight
  exit
fi
if exists batcat; then
  batcat
  exit
fi
if exists bat; then
  bat
  exit
fi

cat
