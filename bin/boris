#!/bin/sh

BORIS_DIR=~/local/src/boris
BORIS=${BORIS_DIR}/bin/boris

if [ ! -f $BORIS ]; then
  echo "*** boris sourcecode($BORIS) is not exists." >&2
  echo "*** clean $BORIS_DIR and clone." >&2
  rm -rf $BORIS_DIR
  git clone https://github.com/d11wtq/boris.git $BORIS_DIR
fi

exec $BORIS $@
